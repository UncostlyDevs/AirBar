<UserControl x:Class="FloatingTaskbarMenu.Controls.SystemTrayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:FloatingTaskbarMenu.Models">
    <StackPanel Margin="8,4">
        <ItemsControl x:Name="TrayIcons">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:TrayIconInfo}">
                    <Button Background="Transparent"
                            BorderThickness="0"
                            Width="28"
                            Height="28"
                            Margin="1"
                            Padding="2"
                            ToolTip="{Binding Tooltip}"
                            Style="{StaticResource AuxiliaryButtonStyle}">
                        <Image Source="{Binding Icon}"
                               Width="20"
                               Height="20"
                               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
