<UserControl x:Class="FloatingTaskbarMenu.Controls.WorkspacesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="248">
    <UserControl.Resources>
        <DataTemplate x:Key="WorkspaceDropdownItemTemplate">
            <Border Padding="8,6">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center"/>
                        <Border Grid.Column="1"
                                Margin="6,0,0,0"
                                Padding="5,1"
                                CornerRadius="{DynamicResource AirBarInnerCornerRadius}"
                                Background="{DynamicResource HoverBrush}"
                                BorderBrush="{DynamicResource MenuBorderBrush}"
                                BorderThickness="1">
                            <TextBlock Text="{Binding Health}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="9"
                                       TextTrimming="CharacterEllipsis"/>
                        </Border>
                    </Grid>
                    <TextBlock Text="{Binding Info}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="10"
                               Margin="0,2,0,0"
                               TextTrimming="CharacterEllipsis"/>
                    <ItemsControl ItemsSource="{Binding WarningLabels}"
                                  Margin="0,5,0,0">
                        <ItemsControl.Style>
                            <Style TargetType="ItemsControl">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasWarnings}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.Style>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,4,0"
                                        Padding="5,1"
                                        CornerRadius="{DynamicResource AirBarInnerCornerRadius}"
                                        Background="{DynamicResource HoverBrush}"
                                        BorderBrush="{DynamicResource AccentBrush}"
                                        BorderThickness="1">
                                    <TextBlock Text="{Binding}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="9"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </DataTemplate>

        <Style x:Key="WorkspaceComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource FlyoutBackgroundBrush}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="ContentTemplate" Value="{StaticResource WorkspaceDropdownItemTemplate}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{DynamicResource AirBarInnerCornerRadius}"
                                Margin="3,2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ItemPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WorkspaceComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Background" Value="{DynamicResource FlyoutBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="{DynamicResource AirBarFontFamily}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="MaxDropDownHeight" Value="228"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid TextElement.Foreground="{TemplateBinding Foreground}">
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          Background="{TemplateBinding Background}"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="Chrome"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{DynamicResource MenuBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="{DynamicResource AirBarInnerCornerRadius}"/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="Chrome" Property="Opacity" Value="0.55"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="10,0,30,0"
                                              IsHitTestVisible="False"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"/>
                            <TextBlock Text="&#xE70D;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="9"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       Margin="0,0,10,0"
                                       IsHitTestVisible="False"/>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Background="{DynamicResource FlyoutBackgroundBrush}"
                                        BorderBrush="{DynamicResource MenuBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource AirBarInnerCornerRadius}">
                                    <ScrollViewer Margin="2"
                                                  CanContentScroll="True"
                                                  SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <Button x:Name="CaptureButton"
                Content="Capture"
                Padding="9,6"
                Margin="0,0,0,8"
                ToolTip="Save the current window layout"
                Click="OnCaptureClick"
                Style="{StaticResource MenuItemButtonStyle}"/>

        <ComboBox x:Name="WorkspaceCombo"
                  Margin="0,0,0,8"
                  Style="{StaticResource WorkspaceComboBoxStyle}"
                  ItemContainerStyle="{StaticResource WorkspaceComboBoxItemStyle}"
                  TextSearch.TextPath="Name"
                  ToolTip="Choose a saved workspace"
                  SelectionChanged="OnWorkspaceSelectionChanged"/>

        <Button x:Name="RestoreButton"
                Content="Restore"
                Margin="0,0,0,6"
                Padding="10,7"
                ToolTip="Restore the selected workspace"
                Click="OnRestoreClick"
                Style="{StaticResource PrimaryMenuItemButtonStyle}"/>

        <UniformGrid Rows="1" Columns="2" Margin="0,0,0,8">
            <Button x:Name="UpdateButton"
                    Content="Update"
                    Margin="0,0,3,0"
                    Padding="8,5"
                    ToolTip="Replace this workspace with the current layout"
                    Click="OnUpdateClick"
                    Style="{StaticResource MenuItemButtonStyle}"/>
            <Button x:Name="DeleteButton"
                    Content="Delete"
                    Margin="3,0,0,0"
                    Padding="8,5"
                    ToolTip="Delete this workspace"
                    Click="OnDeleteClick"
                    Style="{StaticResource MenuItemButtonStyle}"/>
        </UniformGrid>

        <WrapPanel x:Name="WorkspaceStatusChips"/>

        <Border x:Name="LastRestoreChip"
                Visibility="Collapsed"
                Margin="0,2,0,0"
                Padding="6,3"
                CornerRadius="{DynamicResource AirBarInnerCornerRadius}"
                Background="{DynamicResource HoverBrush}"
                BorderBrush="{DynamicResource AccentBrush}"
                BorderThickness="1">
            <TextBlock x:Name="LastRestoreText"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="10"
                       TextWrapping="Wrap"/>
        </Border>
    </StackPanel>
</UserControl>
